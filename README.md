# Yaşar's Shell Setup

Here is my zsh/oh-my-zsh/powerlevel10k configuration and a setup script to automatically configure new VMs.

Set up ubuntu with one line:

```shell
sh -c "$(curl -fsSL https://raw.githubusercontent.com/GodOfKebab/shell-setup/refs/heads/main/host/ubuntu.sh)"
```

